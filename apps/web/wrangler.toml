name = "twitcasting-livefield-web"
compatibility_date = "2026-03-10"
pages_build_output_dir = "dist"

# Emergency cost guard:
# D1 binding disabled after accidental D1 Rows Written overage on the livefield database.
# Do not re-enable without explicit write caps, idempotency, collector auth, retention limits, and Cloudflare budget alerts.
#migrations_dir = "../../db/migrations"
